// import { PATH } from '@/Router';

// console.log(PATH) // undefined why ?!?!?!

export const nicknameRegularExpression = /^[가-힣a-zA-Z0-9]{1,6}$/;

export const couponListDetailPageRegExp = /\/coupon-list\/\d+/;
